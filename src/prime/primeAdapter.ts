// meepo-bot — Prime adapter (stub)
// This is the seam between meepo-bot and meepo-prime.
// Currently stubbed. Replace callPrime body to wire real meepo-prime logic.

/**
 * Invoke the Prime operator workflow with the given input.
 * Stub implementation: echoes input with a [Prime stub] prefix.
 * Replace this with a real meepo-prime call once the Discord route is confirmed.
 */
export async function callPrime(input: string): Promise<string> {
  return `[Prime stub] Received: ${input}`;
}
